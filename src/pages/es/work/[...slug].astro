---
import { type CollectionEntry, getCollection, render } from 'astro:content';

import ContactCTA from '../../../components/ContactCTA.astro';
import Hero from '../../../components/Hero.astro';
import Icon from '../../../components/Icon.astro';
import Pill from '../../../components/Pill.astro';
import BaseLayout from '../../../layouts/BaseLayout.astro';

import ArgoRolloutsSimulator from '../../../components/ArgoRolloutsSimulator';
import GitOpsFlow from '../../../components/GitOpsFlow';
import K8sArchitectureVisualizer from '../../../components/K8sArchitectureVisualizer';
import PodLifecycleSimulator from '../../../components/PodLifecycleSimulator';
import ServiceMeshVisualizer from '../../../components/ServiceMeshVisualizer';
import MetricsDashboardSimulator from '../../../components/MetricsDashboardSimulator';
import ZeroTrustSimulator from '../../../components/ZeroTrustSimulator';
import DatabaseHASimulator from '../../../components/DatabaseHASimulator';
import EdgeNetworkingSimulator from '../../../components/EdgeNetworkingSimulator';
import DisasterRecoverySimulator from '../../../components/DisasterRecoverySimulator';
import TalosOSSimulator from '../../../components/TalosOSSimulator';
import LonghornStorageSimulator from '../../../components/LonghornStorageSimulator';
import ForgejoActionsSimulator from '../../../components/ForgejoActionsSimulator';
import RAGSearchSimulator from '../../../components/RAGSearchSimulator';

interface Props {
	entry: CollectionEntry<'work'>;
}

// This is a dynamic route that generates a page for every Markdown file in src/content/
// Read more about dynamic routes and this `getStaticPaths` function in the Astro docs:
// https://docs.astro.build/en/guides/routing/#dynamic-routes
export async function getStaticPaths() {
	const work = await getCollection('work');
	const spanishWork = work.filter(entry => entry.id.startsWith('es/'));
	return spanishWork.map((entry) => ({
		params: { slug: entry.id.replace('es/', '') },
		props: { entry },
	}));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
---

<BaseLayout title={entry.data.title} description={entry.data.description}>
	<div class="stack gap-20">
		<div class="stack gap-15">
			<header>
				<div class="wrapper stack gap-2">
					<a class="back-link" href="/es/work/"><Icon icon="arrow-left" /> Proyectos</a>
					<Hero title={entry.data.title} align="start">
						<div class="details">
							<div class="tags">
								{entry.data.tags.map((t: string) => <Pill>{t}</Pill>)}
							</div>
							<p class="description">{entry.data.description}</p>
						</div>
					</Hero>
				</div>
			</header>
			<main class="wrapper">
				<div class="stack gap-10 content">
					{entry.data.img && <img src={entry.data.img} alt={entry.data.img_alt || ''} />}
					<div class="content">
						<Content />
					</div>
					
					{(entry.id === 'en/cicd-pipeline' || entry.id === 'es/cicd-pipeline') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<ArgoRolloutsSimulator client:visible />
								</div>
								<div class="island-card">
									<GitOpsFlow client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/kubernetes-cluster' || entry.id === 'es/kubernetes-cluster') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<K8sArchitectureVisualizer client:visible />
								</div>
								<div class="island-card">
									<PodLifecycleSimulator client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/istio-ambient-mesh' || entry.id === 'es/istio-ambient-mesh') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<ServiceMeshVisualizer client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/observability-stack' || entry.id === 'es/observability-stack') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<MetricsDashboardSimulator client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/zero-trust-security' || entry.id === 'es/zero-trust-security') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<ZeroTrustSimulator client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/database-ha' || entry.id === 'es/database-ha') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<DatabaseHASimulator client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/ingress-networking' || entry.id === 'es/ingress-networking') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<EdgeNetworkingSimulator client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/talos-linux' || entry.id === 'es/talos-linux') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<TalosOSSimulator client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/storage-longhorn' || entry.id === 'es/storage-longhorn') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<LonghornStorageSimulator client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/forgejo-actions' || entry.id === 'es/forgejo-actions') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<ForgejoActionsSimulator client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/rag-ai-system' || entry.id === 'es/rag-ai-system') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<RAGSearchSimulator client:visible />
								</div>
							</div>
						</div>
					)}

					{(entry.id === 'en/disaster-recovery' || entry.id === 'es/disaster-recovery') && (
						<div class="islands-demo">
							<h3>Demos Interactivos</h3>
							<div class="islands-grid">
								<div class="island-card">
									<DisasterRecoverySimulator client:visible />
								</div>
							</div>
						</div>
					)}
				</div>
			</main>
		</div>
		<ContactCTA />
	</div>
</BaseLayout>

<style>
	header {
		padding-bottom: 2.5rem;
		border-bottom: 1px solid var(--gray-800);
	}

	.back-link {
		display: none;
	}

	.details {
		display: flex;
		flex-direction: column;
		padding: 0.5rem;
		gap: 1.5rem;
		justify-content: space-between;
		align-items: center;
	}

	.tags {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		gap: 0.5rem;
	}

	.description {
		font-size: var(--text-lg);
		max-width: 54ch;
	}

	.content {
		max-width: 65ch;
		margin-inline: auto;
	}

	.content > :global(* + *) {
		margin-top: 1rem;
	}

	.content :global(h1),
	.content :global(h2),
	.content :global(h3),
	.content :global(h4),
	.content :global(h5) {
		margin: 1.5rem 0;
	}

	.content :global(img) {
		border-radius: 1.5rem;
		box-shadow: var(--shadow-sm);
		background: var(--gradient-subtle);
		border: 1px solid var(--gray-800);
	}

	.content :global(blockquote) {
		font-size: var(--text-lg);
		font-family: var(--font-brand);
		font-weight: 600;
		line-height: 1.1;
		padding-inline-start: 1.5rem;
		border-inline-start: 0.25rem solid var(--accent-dark);
		color: var(--gray-0);
	}

	.back-link,
	.content :global(a) {
		text-decoration: 1px solid underline transparent;
		text-underline-offset: 0.25em;
		transition: text-decoration-color var(--theme-transition);
	}

	.back-link:hover,
	.back-link:focus,
	.content :global(a:hover),
	.content :global(a:focus) {
		text-decoration-color: currentColor;
	}

	@media (min-width: 50em) {
		.back-link {
			display: block;
			align-self: flex-start;
		}

		.details {
			flex-direction: row;
			gap: 2.5rem;
		}

		.content :global(blockquote) {
			font-size: var(--text-2xl);
		}
	}

	.islands-demo {
		margin-top: 2rem;
		padding-top: 2rem;
		border-top: 1px solid var(--gray-800);
	}

	.islands-demo h3 {
		font-size: var(--text-xl);
		margin-bottom: 1.5rem;
		color: var(--gray-0);
	}

	.island-card {
		background: rgba(255, 255, 255, 0.03);
		backdrop-filter: blur(12px);
		-webkit-backdrop-filter: blur(12px);
		border-radius: 1.5rem;
		padding: 1.5rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
		transition: transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease;
		box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
		overflow-x: auto;
	}

	.island-card::-webkit-scrollbar {
		height: 8px;
	}

	.island-card::-webkit-scrollbar-thumb {
		background: rgba(255, 255, 255, 0.2);
		border-radius: 1rem;
	}

	.island-card::-webkit-scrollbar-track {
		background: transparent;
	}

	.island-card:hover {
		transform: translateY(-2px);
		border-color: rgba(255, 255, 255, 0.2);
		box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2), inset 0 1px 0 rgba(255, 255, 255, 0.1);
	}

	@media (min-width: 64em) {
		.islands-grid {
			grid-template-columns: repeat(auto-fit, minmax(min(100%, 350px), 1fr));
		}
	}
</style>
